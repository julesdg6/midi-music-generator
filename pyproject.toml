[project]
name = "midi"
version = "0.1.0"
description = "AI-powered MIDI music generator using LLMs"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "flask>=3.1.2",
    "litellm>=1.80.7",
    "midiutil>=1.2.1",
]
